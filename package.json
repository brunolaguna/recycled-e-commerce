{
  "name": "reciclaon",
  "version": "1.0.0",
  "description": "## Lessons",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\"  \"npm run dev --prefix frontend\" ",
    "build": "cd backend && npm --force install  && npm run build  && cd ../frontend && npm install && npm run build",
    "start": "npm run dev --prefix backend && npm run dev --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brunolaguna/recycled-e-commerce.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/brunolaguna/recycled-e-commerce.git/issues"
  },
  "homepage": "https://github.com/brunolaguna/recycled-e-commerce.git#readme",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
